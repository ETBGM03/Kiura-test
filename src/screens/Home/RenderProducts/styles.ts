import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  textCategory: {
    fontSize: 18,
    fontWeight: 'bold',
    marginVertical: 8,
    textTransform: 'capitalize'
  }
})
